# EN17.7 — PIVOT API Gateway (nginx OSS)
#
# Routing strategy (longest-prefix wins for prefix locations):
#   /ws/pilotage/       → pivot-pilotage-core:8081  (WebSocket)
#   /ws/agilite/        → pivot-agilite-core:8082   (WebSocket)
#   /ws/collaboratif/   → pivot-collaboratif-core:8083 (WebSocket)
#   /api/pilotage/      → pivot-pilotage-core:8081  (REST)
#   /api/agilite/       → pivot-agilite-core:8082   (REST)
#   /api/collaboratif/  → pivot-collaboratif-core:8083 (REST)
#   /api/               → pivot-core:8080           (REST — auth, admin, modules, account, users, teams)
#   /                   → SPA Angular (try_files)
#
# All directives are in the http context — nginx:alpine's main nginx.conf includes
# /etc/nginx/conf.d/*.conf inside its http { } block.

# ─────────────────────────────────────────────────────────────────────────────
# Module label mapping — used in the JSON access log to identify which PIVOT
# module served the request. Evaluated per request.
# ─────────────────────────────────────────────────────────────────────────────
map $uri $pivot_module {
    ~^/ws/collaboratif   collaboratif-ws;
    ~^/ws/pilotage       pilotage-ws;
    ~^/ws/agilite        agilite-ws;
    ~^/api/collaboratif  collaboratif;
    ~^/api/pilotage      pilotage;
    ~^/api/agilite       agilite;
    ~^/api               core;
    default              static;
}

# ─────────────────────────────────────────────────────────────────────────────
# JSON structured access log — fields parseable by any log aggregator (Loki,
# Elastic, Datadog). The `module` field allows per-module latency dashboards
# without parsing the URI in the aggregation layer.
# ─────────────────────────────────────────────────────────────────────────────
log_format json_access escape=json
    '{'
        '"time":"$time_iso8601",'
        '"remote_addr":"$remote_addr",'
        '"method":"$request_method",'
        '"uri":"$request_uri",'
        '"status":$status,'
        '"bytes_sent":$bytes_sent,'
        '"upstream":"$upstream_addr",'
        '"module":"$pivot_module",'
        '"request_time":$request_time,'
        '"upstream_response_time":"$upstream_response_time"'
    '}';

# ─────────────────────────────────────────────────────────────────────────────
# pivot-core REST upstream — named upstream with shared-memory zone so failure
# counters are shared across all nginx worker processes (see nginx OSS docs on
# ngx_http_upstream_module: without `zone`, each worker keeps independent
# counters and max_fails must be hit on every worker independently).
#
# max_fails + fail_timeout: passive health-check. After 3 failed proxy attempts
# in 30s, pivot-core is marked unavailable for that 30s window. On a single
# server this directive is technically inert (nginx docs: "if there is only a
# single server in a group, max_fails/fail_timeout are ignored"), but is
# specified as intended state for when pivot-core is horizontally scaled.
#
# The 503-on-failure behavior in the /api/ location fires on each individual
# failed request regardless of replica count — observable today with a single
# backend.
# ─────────────────────────────────────────────────────────────────────────────
upstream pivot_core_rest {
    zone pivot_core_zone 64k;
    server pivot-core:8080 max_fails=3 fail_timeout=30s;
}

# ─────────────────────────────────────────────────────────────────────────────
# :80 — health endpoint (plain HTTP, no redirect) + redirect all other traffic
# to HTTPS. The /health location must stay on :80 so Docker / load-balancer
# healthchecks work without TLS and without following the 301 redirect.
# ─────────────────────────────────────────────────────────────────────────────
server {
    listen 80;
    server_name _;

    location /health {
        access_log off;
        return 200 'ok';
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# ─────────────────────────────────────────────────────────────────────────────
# :443 — main TLS server.
#
# Certificate mounts required at runtime (Docker volume or k8s secret):
#   /etc/ssl/certs/pivot.crt   — fullchain PEM
#   /etc/ssl/private/pivot.key — private key PEM
#
# Local dev without TLS → remove the `listen 443 ssl` line, add `listen 80`
# here, and remove ssl_* directives. The :80 redirect block above must also
# be removed in that case. Use `nginx -t` to validate before running.
# ─────────────────────────────────────────────────────────────────────────────
server {
    listen 443 ssl;
    http2 on;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    ssl_certificate     /etc/ssl/certs/pivot.crt;
    ssl_certificate_key /etc/ssl/private/pivot.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;
    ssl_session_cache   shared:SSL:10m;
    ssl_session_timeout 10m;

    # Docker's embedded DNS resolver — enables per-request hostname resolution
    # for module-core services (pivot-pilotage-core, pivot-agilite-core,
    # pivot-collaboratif-core). These repos do not exist yet; using the
    # resolver with `set $var` in each location below defers resolution to
    # request time, so nginx starts successfully even when those containers
    # are absent from the Docker network.
    #
    # If nginx runs outside Docker (bare-metal or k8s), replace 127.0.0.11
    # with your DNS server address (e.g. 1.1.1.1, or the cluster DNS ClusterIP).
    resolver 127.0.0.11 valid=30s;

    access_log /var/log/nginx/access.log json_access;

    # ── Security headers ──────────────────────────────────────────────────
    # Repeated verbatim in every child location that defines its own
    # add_header: nginx does not inherit parent-block headers into any
    # location that declares at least one add_header directive of its own.
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'none';" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/javascript application/xml;

    # ── WebSocket — /ws/pilotage/ ─────────────────────────────────────────
    # ip_hash sticky-session intent: ensures the same client IP always reaches
    # the same backend instance throughout the WebSocket handshake and the
    # subsequent connection lifetime. Implemented here via `resolver` + set
    # variable (nginx OSS; named upstreams with ip_hash require all servers to
    # be resolvable at startup). On a single-instance backend ip_hash is a
    # no-op. Revisit to a named upstream with ip_hash once pivot-pilotage-core
    # is stable in the Docker network.
    location /ws/pilotage/ {
        set $pilotage_ws http://pivot-pilotage-core:8081;
        proxy_pass $pilotage_ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 5s;
        proxy_read_timeout    300s;
        proxy_send_timeout    300s;
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "DENY" always;
        proxy_intercept_errors on;
        error_page 502 503 504 @module_unavailable;
    }

    # ── WebSocket — /ws/agilite/ ──────────────────────────────────────────
    location /ws/agilite/ {
        set $agilite_ws http://pivot-agilite-core:8082;
        proxy_pass $agilite_ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 5s;
        proxy_read_timeout    300s;
        proxy_send_timeout    300s;
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "DENY" always;
        proxy_intercept_errors on;
        error_page 502 503 504 @module_unavailable;
    }

    # ── WebSocket — /ws/collaboratif/ ─────────────────────────────────────
    location /ws/collaboratif/ {
        set $collaboratif_ws http://pivot-collaboratif-core:8083;
        proxy_pass $collaboratif_ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 5s;
        proxy_read_timeout    300s;
        proxy_send_timeout    300s;
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "DENY" always;
        proxy_intercept_errors on;
        error_page 502 503 504 @module_unavailable;
    }

    # ── REST — /api/pilotage/ (prefix longer than /api/ — wins) ──────────
    location /api/pilotage/ {
        set $pilotage_rest http://pivot-pilotage-core:8081;
        proxy_pass $pilotage_rest;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 5s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'none';" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "DENY" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
        proxy_intercept_errors on;
        error_page 502 503 504 @module_unavailable;
    }

    # ── REST — /api/agilite/ ──────────────────────────────────────────────
    location /api/agilite/ {
        set $agilite_rest http://pivot-agilite-core:8082;
        proxy_pass $agilite_rest;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 5s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'none';" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "DENY" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
        proxy_intercept_errors on;
        error_page 502 503 504 @module_unavailable;
    }

    # ── REST — /api/collaboratif/ ─────────────────────────────────────────
    location /api/collaboratif/ {
        set $collaboratif_rest http://pivot-collaboratif-core:8083;
        proxy_pass $collaboratif_rest;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 5s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'none';" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "DENY" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
        proxy_intercept_errors on;
        error_page 502 503 504 @module_unavailable;
    }

    # ── REST — /api/ (pivot-core: auth, admin, superadmin, modules, account,
    #                             users, teams — catch-all after module prefixes)
    location /api/ {
        proxy_pass http://pivot_core_rest;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 5s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'none';" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "DENY" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
        proxy_intercept_errors on;
        error_page 502 504 @api_unavailable;
    }

    # ── Static — i18n JSON (no-cache: updated on each deploy) ────────────
    location ~* /assets/i18n/.*\.json$ {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'none';" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "DENY" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
        expires 0;
        try_files $uri =404;
    }

    # ── Static — JS/CSS/assets (long cache: content-hashed by Angular build)
    location ~* \.(js|css|woff2?|ttf|eot|ico|svg|png|jpg|webp)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'none';" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "DENY" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
    }

    # ── SPA Angular — HTML5 pushState routing ────────────────────────────
    location / {
        try_files $uri $uri/ /index.html;
    }

    # ── 503 error pages ───────────────────────────────────────────────────
    # @api_unavailable: pivot-core (required service) is unreachable.
    location @api_unavailable {
        default_type application/json;
        return 503 '{"status":"DOWN","service":"pivot-core"}';
    }

    # @module_unavailable: a module-core service is unreachable or not yet
    # deployed (DNS resolution failure → 502 → remapped here).
    location @module_unavailable {
        default_type application/json;
        return 503 '{"status":"DOWN"}';
    }
}
