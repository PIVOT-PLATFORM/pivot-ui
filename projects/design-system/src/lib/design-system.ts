/**
 * @pivot/design-system — Angular workspace library (EN17.8)
 *
 * Ce fichier est conservé pour compatibilité avec le générateur ng-packagr.
 * Les exports réels sont dans public-api.ts → components/, tokens/, cdk/, scss/.
 */
export {};
